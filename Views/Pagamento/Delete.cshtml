@model ProjetoBancoDados.Models.Pagamento

@{
    ViewData["Title"] = "Deletar";
}

<h1>Deletar</h1>

<h3>Você tem certeza que deseja deletar isso?</h3>
<div>
    <h4>Pagamento</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PagamentoNome)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PagamentoNome)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="PagamentoId" />
        <input type="submit" value="Deletar" class="btn-danger" />
        <a asp-action="Index" class="btn-primary">Voltar à Lista</a>
    </form>
</div>
