@model ProjetoBancoDados.Models.TipoHospedagem

@{
    ViewData["Title"] = "Editar";
}

<h1>Editar</h1>

<h4>Tipo Hospedagem</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TipoHospedagemId" />
            <div class="form-group">
                <label asp-for="TipoHospedagemNome" class="control-label"></label>
                <input asp-for="TipoHospedagemNome" class="form-control" />
                <span asp-validation-for="TipoHospedagemNome" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Salvar" class="btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn-primary">Voltar à Lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
